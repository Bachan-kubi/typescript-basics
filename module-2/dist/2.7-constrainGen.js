"use strict";
{
    const person1 = "bike";
    // get property value 
    const getPropKey = (obj, key) => {
        return obj[key];
    };
    const user = { name: 'i', kam: "ai", dam: "ai" };
    const car = { name: 'toyota', model: "nai", dam: 255555 };
    const resutl1 = getPropKey(user, "kam");
    const resutl2 = getPropKey(car, "model");
}
